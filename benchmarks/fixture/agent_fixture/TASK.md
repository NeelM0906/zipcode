# Agent task

Repair `retry_batch.py` so the complete test suite passes.

Constraints:

- Do not modify `test_retry_batch.py`.
- Preserve the public API and type annotations.
- Use only the Python standard library.
- Run `python3 -m unittest -v` before finishing.
- Your final response must contain `CODEX_AGENT_BENCH_OK` if and only if all
  tests pass.
